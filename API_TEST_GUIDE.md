# دليل اختبار وإصلاح الـ Backend - تعليمات سريعة

## الخطوة 1: تشغيل الـ Backend

```powershell
cd C:\Users\ASUS\OneDrive\Desktop\samah.storef\samah.store-Project
.\mvnw.cmd spring-boot:run
```

انتظر حتى ترى:
```
Started DemoApplication in X.XXX seconds
```

## الخطوة 2: تشغيل سكريبت الاختبار

في نافذة PowerShell جديدة:

```powershell
cd C:\Users\ASUS\OneDrive\Desktop\samah.storef\samah.store-Project
.\api-test.ps1
```

## الخطوة 3: فحص النتائج

السكريبت سيعرض:
- ✅ الـ endpoints التي تعمل
- ❌ الـ endpoints التي فشلت
- نسبة النجاح (Pass Rate)

## متطلبات قبل الاختبار

### إنشاء مستخدم Admin (إذا لم يكن موجودًا)

استخدم أداة قاعدة البيانات أو endpoint التسجيل لإنشاء:

```json
POST /api/auth/register
{
  "username": "admin",
  "email": "admin@samah.store",
  "password": "admin123"
}
```

ثم قم بترقية الدور في قاعدة البيانات:
```sql
UPDATE store.users SET role = 'ADMIN' WHERE username = 'admin';
```

### إنشاء مستخدم Customer (للاختبار)

```json
POST /api/auth/register
{
  "username": "customer",
  "email": "customer@test.com",
  "password": "customer123"
}
```

## المشاكل المتوقعة وكيفية إصلاحها

### 1. فشل تسجيل الدخول (401)

**السبب**: لا يوجد مستخدمين في قاعدة البيانات

**الحل**: 
- أنشئ المستخدمين كما في الأعلى
- أو عدّل السكريبت ليستخدم credentials موجودة

### 2. فشل endpoints الـ Admin (403)

**السبب**: المستخدم ليس ADMIN

**الحل**:
```sql
UPDATE store.users SET role = 'ADMIN' WHERE username = 'admin';
```

### 3. فشل Get Products/Categories (404 أو 500)

**السبب**: لا توجد بيانات في قاعدة البيانات

**الحل**: أضف بيانات تجريبية عبر Admin UI أو SQL

### 4. فشل Cart/Addresses (500)

**السبب محتمل**: 
- مشكلة في principal resolution
- عدم وجود cart للمستخدم

**الحل**: سيتم إصلاحها بناءً على الـ error المحدد

### 5. فشل Metrics (404)

**السبب**: الـ endpoint غير موجود أو الـ route خاطئ

**الحل**: التحقق من AdminMetricsController

## تفسير النتائج

### Pass Rate >= 90%
🎉 النظام جاهز للإنتاج

### Pass Rate 70-89%
⚠️ يحتاج انتباه - بعض المشاكل البسيطة

### Pass Rate < 70%
🚨 حرج - يحتاج إصلاح فوري

## الخطوة التالية

بعد تشغيل السكريبت:

1. إذا فشل أي endpoint:
   - انظر إلى الـ Backend logs
   - ابحث عن الـ stacktrace
   - حدد السبب الجذري
   - طبق الإصلاح الأدنى

2. أعد تشغيل السكريبت للتحقق من الإصلاح

3. كرر حتى تصل Pass Rate إلى 100%

## ملفات مرجعية

- `api-test.ps1` - سكريبت الاختبار الرئيسي
- `FIX_SUMMARY_ORDER_STATUS.md` - ملخص إصلاحات Order Status
- `CRITICAL_FIXES_APPLIED.md` - تفاصيل الإصلاحات المطبقة

## الأوامر السريعة

```powershell
# بناء المشروع
.\mvnw.cmd clean package -DskipTests

# تشغيل Backend
.\mvnw.cmd spring-boot:run

# تشغيل الاختبار (نافذة منفصلة)
.\api-test.ps1

# إيقاف Backend
Ctrl+C
```

## التحقق من قاعدة البيانات

```sql
-- التحقق من وجود مستخدمين
SELECT id, username, email, role FROM store.users;

-- التحقق من وجود منتجات
SELECT COUNT(*) FROM store.products;

-- التحقق من وجود طلبات
SELECT COUNT(*) FROM store.orders;
```

---

**ملاحظة هامة**: السكريبت يختبر الـ endpoints الموجودة فعليًا فقط. لن يخترع endpoints جديدة.

